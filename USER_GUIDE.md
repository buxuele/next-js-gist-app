# 📚 我的知识库 - 用户使用指南

## 🎯 应用概述

"我的知识库" 是一个基于 Next.js 的个人代码片段管理工具，支持多用户系统，每个用户只能管理自己的代码片段。

### ✨ 主要特性

- 🔐 **GitHub OAuth 登录** - 只支持 GitHub 账号登录，无需注册
- 👤 **多用户隔离** - 每个用户只能看到和管理自己的代码片段
- 🎨 **语法高亮** - 自动识别代码语言并提供精准的语法高亮
- 📱 **响应式设计** - 完美支持桌面、平板和移动设备
- ⚡ **实时操作** - 创建、编辑、删除代码片段，实时生效
- 🔍 **智能搜索** - 支持按文件名、描述搜索（即将推出）

## 🚀 如何使用

### 1. 登录应用

1. **访问应用网址**：https://your-app-domain.vercel.app
2. **自动跳转到登录页面**（如果未登录）
3. **点击 "使用 GitHub 登录"**
4. **授权应用访问你的 GitHub 账号**
5. **登录成功后自动跳转到主页**

### 2. 管理代码片段

#### 创建新的代码片段

1. **点击导航栏的 "添加" 按钮**
2. **填写表单**：
   - **文件名**：例如 `script.py`、`utils.js`（可选，会自动推测）
   - **描述**：简短描述这个代码片段的用途（必填）
   - **内容**：粘贴或输入你的代码（必填）
3. **点击 "保存" 按钮**

#### 编辑现有代码片段

1. **在代码片段卡片上点击 "修改" 按钮**
2. **在弹出的对话框中修改内容**
3. **点击 "保存" 按钮**

#### 删除代码片段

1. **在代码片段卡片上点击 "删除" 按钮**
2. **在确认对话框中点击 "确定"**

#### 复制代码片段

1. **在代码片段卡片上点击 "复制" 按钮**
2. **代码内容会自动复制到剪贴板**

#### 展开/收起长代码

- 对于超过 15 行的代码，会自动折叠
- 点击卡片底部的 "显示更多" 按钮展开
- 点击 "收起" 按钮折叠

### 3. 用户账户管理

#### 查看用户信息

- 点击导航栏右侧的用户头像和用户名
- 下拉菜单显示当前登录用户信息

#### 登出

- 点击用户下拉菜单中的 "登出" 按钮
- 会自动跳转到登录页面

## 🔒 隐私和安全

### 数据隔离

- **完全隔离**：每个用户只能看到自己的代码片段
- **权限控制**：无法访问其他用户的数据
- **安全认证**：所有 API 请求都需要用户认证

### 数据存储

- **云端存储**：数据存储在 Neon PostgreSQL 数据库中
- **自动备份**：Neon 提供自动备份功能
- **SSL 加密**：所有数据传输都使用 SSL 加密

### GitHub OAuth 安全

- **最小权限**：只获取必要的用户信息（用户名、邮箱、头像）
- **安全令牌**：使用 JWT 令牌管理用户会话
- **自动过期**：会话令牌会自动过期，需要重新登录

## 🛠️ 技术规格

### 支持的代码语言

应用会自动识别以下编程语言并提供语法高亮：

- **Web 开发**：JavaScript, TypeScript, HTML, CSS
- **后端开发**：Python, Java, C#, PHP, Ruby
- **系统编程**：C, C++, Rust, Go
- **脚本语言**：Bash, PowerShell, Batch
- **数据格式**：JSON, XML, YAML, SQL
- **标记语言**：Markdown

### 浏览器兼容性

- **现代浏览器**：Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **移动浏览器**：iOS Safari 14+, Chrome Mobile 90+

### 文件大小限制

- **单个代码片段**：最大 50KB
- **描述长度**：最大 200 字符
- **文件名长度**：最大 50 字符

## 📱 移动端使用

### 响应式布局

- **手机端**：单列布局，适合小屏幕浏览
- **平板端**：双列布局，充分利用屏幕空间
- **桌面端**：四列网格布局，最佳浏览体验

### 移动端操作

- **触摸友好**：所有按钮都针对触摸操作优化
- **手势支持**：支持滑动、点击等手势
- **键盘适配**：虚拟键盘弹出时自动调整布局

## 🔧 常见问题

### Q: 为什么只能用 GitHub 登录？

A: 为了简化用户管理和提高安全性，我们选择只支持 GitHub OAuth 登录。这样可以：

- 避免密码管理的安全风险
- 利用 GitHub 的安全基础设施
- 简化注册流程

### Q: 我的数据安全吗？

A: 是的，你的数据是安全的：

- 数据存储在可靠的 Neon PostgreSQL 云数据库中
- 所有数据传输都使用 SSL 加密
- 每个用户的数据完全隔离
- 定期自动备份

### Q: 可以导出我的代码片段吗？

A: 目前暂不支持批量导出功能，但你可以：

- 逐个复制代码片段内容
- 使用浏览器的打印功能保存为 PDF
- 我们计划在未来版本中添加导出功能

### Q: 支持团队协作吗？

A: 目前不支持团队协作功能，每个用户只能管理自己的代码片段。团队协作功能在我们的未来规划中。

### Q: 有使用限制吗？

A: 目前没有严格的使用限制，但建议：

- 合理使用存储空间
- 不要存储敏感信息（如密码、API 密钥）
- 遵守服务条款

## 📞 技术支持

### 报告问题

如果遇到技术问题，请：

1. **检查网络连接**
2. **刷新浏览器页面**
3. **清除浏览器缓存**
4. **尝试重新登录**

### 功能建议

我们欢迎你的功能建议和反馈：

- 通过 GitHub Issues 提交建议
- 发送邮件到支持邮箱
- 在社交媒体上联系我们

## 🚀 未来规划

### 即将推出的功能

- 🔍 **高级搜索** - 支持标签、内容搜索
- 📁 **文件夹组织** - 按项目或类别组织代码片段
- 📤 **导入/导出** - 支持批量导入导出
- 🏷️ **标签系统** - 为代码片段添加标签
- 📊 **使用统计** - 查看使用情况和统计信息
- 🌙 **主题切换** - 支持明暗主题切换
- ⌨️ **快捷键** - 支持键盘快捷键操作

### 长期规划

- 👥 **团队协作** - 支持团队共享代码片段
- 🔗 **公开分享** - 支持公开分享代码片段
- 📱 **移动应用** - 开发原生移动应用
- 🤖 **AI 助手** - 集成 AI 代码助手功能

---

**感谢使用我的知识库！** 🎉

如果你觉得这个工具有用，欢迎分享给其他开发者朋友。
